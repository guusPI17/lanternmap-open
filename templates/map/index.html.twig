{% block body %}
    <div class="scrollMaps">
        <ol class="listMap">
            {% for map in maps %}
                <li><a href="{{ path('map_show', {'id': map.id}) }}">{{ map.name }}</a>
                    {{ include('map/_delete_form.html.twig') }}
                </li>
            {% endfor %}
        </ol>
    </div>
{% endblock %}
